import std

# This MUST fail to compile: closure escapes via return without @heap.
# Compiler should refuse with a clear message pointing the user to @heap.

fun make_bad(n: i32) -> fun(i32) -> i32:
    return |x: i32| -> i32 x + n
