# Test list<i32>
mylist: list<i32> = [1, 2, 3]
for item in mylist:
    IOOps.io_println(Convert.i64_to_str(item))

# Test list<str>
names: list<str> = ["Ali", "Sara", "Omar"]
for name in names:
    print(name)

# Test array<i32>
nums: array<i32> = [10, 20, 30]
for n in nums:
    IOOps.io_println(Convert.i64_to_str(n))
